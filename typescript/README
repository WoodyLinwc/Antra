# Install TypeScript as a dev dependency
npm install --save-dev typescript

# Using project-based TypeScript
npx tsc filename.ts

# Generate a default tsconfig.json
tsc --init

# Compile
tsc examples.ts 